NEXT_PUBLIC_API_URL=http://localhost:4000/api

# ImageKit public URL endpoint (no secret — never put the private key here).
# Serves citizen photo URLs per the shared/lib/imagekit.ts convention.
NEXT_PUBLIC_IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/your-imagekit-id